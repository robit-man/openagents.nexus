# Prevent native module compilation during install.
# node-datachannel (transitive dep of @libp2p/webrtc) requires cmake + prebuild
# which fails in Cloudflare's build environment. We don't use WebRTC transport
# (TCP + WebSocket + Circuit Relay cover all our needs).
ignore-scripts=true
